package ${package}.modules.${module}.dao;

import com.bottle.pay.common.mapper.BottleBaseMapper;
import ${package}.modules.api.entity.${className}Entity;
import org.apache.ibatis.annotations.Mapper;

/**
 * ${comments}
 * @author ${author}<${email}>
 */
@Mapper
public interface ${className}Mapper extends BottleBaseMapper<${className}Entity> {
	
}
